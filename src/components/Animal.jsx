import styled from 'styled-components';

const AnimalContainer = styled.section`
`

const Animal = () => {
  return (
   <AnimalContainer>
    <section id='animal'>
    </section>
   </AnimalContainer>
  )
}

export default Animal;